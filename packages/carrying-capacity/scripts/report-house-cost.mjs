import {mkdir, writeFile} from 'node:fs/promises';
import {dirname, resolve} from 'node:path';
import {buildHouseCostPresentationContract, calculateHouseCost, HOUSE_COST_CONTRACT_VERSION} from '../src/house-cost.mjs';

const outputRoot = resolve('packages/education-web/public/generated/house-cost');
const evidenceRoot = resolve('know/produce/house-cost');
const contract = buildHouseCostPresentationContract();
const cad = (value) => `$${Number(value ?? 0).toLocaleString('en-CA', {minimumFractionDigits: 0, maximumFractionDigits: 0})}`;
const money = (value) => `$${Number(value ?? 0).toLocaleString('en-CA', {minimumFractionDigits: 2, maximumFractionDigits: 2})}`;
const hours = (value) => `${Number(value ?? 0).toLocaleString('en-CA', {maximumFractionDigits: 1})} h`;
const central = contract.central;
const modes = ['owner_builder', 'mixed_labour', 'contractor_built'].map((labourMode) => {
  const result = calculateHouseCost({labourMode});
  return {labourMode, label: result.labour.label, cash: result.totals.upfront_cash_required_cad, capital: result.totals.completed_dwelling_capital_cad, ownerHours: result.labour.owner_hours, paidHours: result.labour.paid_hours, financing: result.financing.monthly_debt_service_cad};
});
const componentRows = central.components.map((row) => `| ${row.label} | ${row.quantity.toLocaleString('en-CA', {maximumFractionDigits: 2})} | ${row.unit} | ${money(row.unit_rate_cad)} | ${money(row.material_cost_cad)} | ${hours(row.labour_hours_total)} | ${money(row.cash_cost_cad)} | ${row.status} |`).join('\n');
const diameterRows = contract.diameter_sensitivity.map((row) => `| ${row.label} | ${row.usable_floor_area_m2.toFixed(1)} | ${money(row.completed_dwelling_capital_cad)} | ${money(row.cost_per_usable_m2_cad)} | ${row.thresholds.join(', ') || 'none'} |`).join('\n');
const layoutRows = contract.layout_comparison.map((row) => `| ${row.label} | ${row.usable_floor_area_m2.toFixed(1)} | ${money(row.completed_dwelling_capital_cad)} | ${money(row.cost_per_usable_m2_cad)} | ${hours(row.owner_labour_hours)} | ${hours(row.paid_labour_hours)} |`).join('\n');
const modeRows = modes.map((row) => `| ${row.label} | ${money(row.cash)} | ${money(row.capital)} | ${hours(row.ownerHours)} | ${hours(row.paidHours)} | ${money(row.financing)}/month |`).join('\n');
const markdown = `# ARC House Cost Calculator\n\nGenerated from contract ${HOUSE_COST_CONTRACT_VERSION} on 2026-09-05. This is a planning model for a completed four-season yurt dwelling; land lease, shared infrastructure operating charges and household operating expenses are separate.\n\n## Central reference\n\n- Geometry: ${central.geometry.inputs.diameter_m} m diameter (${central.geometry.gross_floor_area_m2.toFixed(2)} m² gross; ${central.geometry.usable_floor_area_m2.toFixed(2)} m² usable after explicit deductions)\n- Servicing: ${central.servicing.label}\n- Upfront cash required: ${money(central.totals.upfront_cash_required_cad)}\n- Completed dwelling capital including owner-labour value: ${money(central.totals.completed_dwelling_capital_cad)}\n- Illustrative financing: ${money(central.financing.monthly_debt_service_cad)}/month at ${central.financing.interest_rate_annual * 100}% interest, ${central.financing.amortization_years} year amortization, ${central.financing.down_payment_cad.toLocaleString('en-CA')} down\n\nThe legacy ARC central benchmark was ${cad(central.legacy_reconciliation.legacy_central_cad)}. The new component model's core capital before delivery, equipment hire, design, permits, tax and contingency is ${money(central.legacy_reconciliation.model_core_capital_before_soft_tax_contingency_cad)}; its completed economic capital is ${money(central.legacy_reconciliation.model_completed_economic_capital_cad)}. The difference is exposed rather than hidden.\n\n## Component audit\n\n| Component | Quantity | Unit | Unit rate | Materials / allowance | Labour | Cash cost | Evidence status |\n| --- | ---: | --- | ---: | ---: | ---: | ---: | --- |\n${componentRows}\n\nTaxes and contingency are shown as separate additions: ${money(central.totals.taxes_cad)} tax/HST allowance and ${money(central.totals.contingency_cad)} contingency. Owner labour is ${hours(central.labour.owner_hours)} (${money(central.labour.owner_labour_imputed_cad)} imputed); paid labour is ${hours(central.labour.paid_hours)} (${money(central.labour.paid_labour_cash_cad)} cash).\n\n## Labour modes\n\n| Mode | Upfront cash | Completed capital | Owner hours | Paid hours | Illustrative finance |\n| --- | ---: | ---: | ---: | ---: | ---: |\n${modeRows}\n\nOwner-builder cash is lower because owner labour is contributed, not because that work disappears. Professional/design and approval work remains paid.\n\n## Diameter sensitivity\n\n| Diameter | Usable m² | Completed capital | Cost / usable m² | Applied thresholds |\n| --- | ---: | ---: | ---: | --- |\n${diameterRows}\n\n## Layout comparison\n\n| Layout | Usable m² | Completed capital | Cost / usable m² | Owner hours | Paid hours |\n| --- | ---: | ---: | ---: | ---: | ---: |\n${layoutRows}\n\n## Accounting and evidence\n\n- Shell is the platform/foundation, frame, roof, insulation, weatherproofing, windows, doors and any upper-floor structure.\n- Insulated/heated structure adds interior finish, heating, ventilation, stairs and guards.\n- Completed dwelling adds kitchen, bathroom, distributed household systems, logistics, equipment, design, permits, tax and contingency.\n- The ARC household utility package is carried forward once: water/plumbing CAD 3,940, sanitation/greywater CAD 2,000, hot water CAD 2,000 and electrical CAD 3,300. Generic well/septic/grid options remain alternatives.\n- Centralized servicing removes household utility capital and reports unresolved shared-infrastructure quotation requirements; it is not silently added to this dwelling.\n- A custom quote overrides the financing headline while any unallocated difference remains visible.\n\nThe strongest evidence supports geometry/specification boundaries and Ontario permit/servicing obligations. Component rates, structural thresholds, labour rates, HST treatment and site logistics remain planning estimates or quotation-required inputs.\n\n## Sources\n\n${contract.sources.map((source) => `- [${source.institution}: ${source.title}](${source.url}) - ${source.classification}. ${source.note}`).join('\n')}\n`;
await mkdir(outputRoot, {recursive: true});
await mkdir(evidenceRoot, {recursive: true});
await writeFile(resolve(outputRoot, 'cost-model.json'), JSON.stringify(contract, null, 2) + '\n');
await writeFile(resolve(outputRoot, 'cost-model.md'), markdown);
await writeFile(resolve(evidenceRoot, 'cost-model.json'), JSON.stringify(contract, null, 2) + '\n');
await writeFile(resolve(evidenceRoot, 'cost-model.md'), markdown);
console.log(`wrote ARC house cost model ${HOUSE_COST_CONTRACT_VERSION}`);
