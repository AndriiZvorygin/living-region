{
  "name": "arc-carrying-capacity-model",
  "version": "0.1.0",
  "private": true,
  "description": "Reproducible audit and extension of the ARC one-hectare-per-adult carrying-capacity model",
  "type": "module",
  "scripts": {
    "extract": "node scripts/extract-ods.mjs",
    "build": "node scripts/build-summary.mjs",
    "build:evidence": "node scripts/build-summary.mjs",
    "test": "node --test",
    "all": "npm run extract && npm run build && npm test"
  }
}
